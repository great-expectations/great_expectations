Finally, you can install Great Expectations by running the appropriate pip command below:

```console title="Terminal Command"
python -m pip install great_expectations
```

or

```console title="Terminal Command"
python3 -m pip install great_expectations
```