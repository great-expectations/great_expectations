import TipWhichDataConnectorToUse from '../components/_tip_which_data_connector_to_use.mdx'

<!-- props.backend: The backend used by the datasource's Execution Engine. -->
<p>
    For each Data Connector configuration, you will need to specify which type of Data Connector you will be using.  For {props.backend}, the most likely ones will be the <code>InferredAssetSqlDataConnector</code>, the <code>ConfiguredAssetSqlDataConnector</code>, and the <code>RuntimeDataConnector</code>.
</p>

<TipWhichDataConnectorToUse />