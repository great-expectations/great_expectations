<!-- props.data_connector_type: string value of the data connector's class name. -->
<!-- props.data_connector_name: string value of the name assigned to the data connector in previous examples. -->
<!-- props.inferred: boolean value that determines if the base key list should be `class_name`, `splitter_method`, `splitter_kwargs`, `introspection_directives` (true) -->
<!-- props.configured: boolean value that determines if the base key list should be `class_name`, `assets` (true) -->
<!-- props.runtime: boolean value that determines if the base key list should be `class_name`, `batch_identifiers` (true) -->

<div>
    <p>
        When defining an <code>{props.data_connector_type}</code> you will need to provide values for { props.runtime && <span>at least two required (and up to an additional three optional)</span> || props.configured && <span>one required (and up to an additional three optional)</span> || <span>two</span> } keys in the Data Connector's configuration dictionary (the currently empty dictionary that corresponds to <code>"{props.data_connector_name}"</code> in the example above).  These key/value pairs consist of:
    </p>
    <ul>
        <li>
            <code>class_name</code>: The name of the Class that will be instantiated for this <code>DataConnector</code>.
        </li>
        { props.inferred &&
            <span>
                <li>
                    <code>splitter_method</code> (Optional): A string that identifies the method by which Data Assets should be split into Batches.
                </li>
                <li>
                    <code>splitter_kwargs</code> (Optional): A dictionary of parameters for use by the `splitter_method`, if a `splitter_method` is defined.
                </li>
                <li>
                    <code>introspection_directives (Optional?)</code>: TODO
                </li>
            </span>
        || props.configured &&
            <span>
                <li>
                    <code>assets</code>: A list of strings that will be used as keys for identifying metadata that the user provides for the returned Batch.
                </li>
                <li>
                    <code>splitter_method (optional)</code>: A list of strings that will be used as keys for identifying metadata that the user provides for the returned Batch.
                </li>
                <li>
                    <code>splitter_kwargs</code> (Optional): A dictionary of parameters for use by the `splitter_method`, if a `splitter_method` is defined.
                </li>
                <li>
                    <code>introspection_directives (Optional)</code>: TODO
                </li>
            </span>
        || props.runtime &&
            <span>
                <li>
                    <code>batch_identifiers</code>: A list of identifying information that you will provide along with a Batch Request so that the data returned by the Batch Request can be referenced in the future.
                </li>
            </span>
        }
    </ul>
</div>
