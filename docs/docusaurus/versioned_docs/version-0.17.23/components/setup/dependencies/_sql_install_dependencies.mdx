import CodeBlock from '@theme/CodeBlock';

<p>To install Great Expectations with the optional dependencies needed to work with {props.database_name} we execute the following pip command from the terminal:</p>

<CodeBlock language="text" title="Terminal input">
    pip install 'great_expectations[{props.install_key}]'
</CodeBlock>