<p>In this guide we will demonstrate how to use {props.execution_engine} to connect to data stored in files on a filesystem. In this example we will specifically be connecting to data in <code>.csv</code> format. {props.execution_engine === 'Pandas' && "However, GX supports most read methods available through Pandas."}</p>