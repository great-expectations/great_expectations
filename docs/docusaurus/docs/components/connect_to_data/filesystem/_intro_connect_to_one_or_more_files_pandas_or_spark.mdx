<p>Use {props.execution_engine} to connect to data stored in files on a filesystem. The following examples connect to <code>.csv</code> data. {props.execution_engine === 'Pandas' && "However, GX supports most of the Pandas read methods."}</p>