import GxData from '/docs/components/_data.jsx'

You can check your version of Python by running:

```console title="Terminal command"
python --version
```

<p>GX currently supports Python versions {GxData.min_python} to {GxData.max_python}</p>