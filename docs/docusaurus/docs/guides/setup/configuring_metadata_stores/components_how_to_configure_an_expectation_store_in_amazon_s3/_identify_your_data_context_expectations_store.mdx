import TechnicalTag from '@site/docs/term_tags/_tag.mdx';

You can add a new <TechnicalTag tag="expectation_store" text="Expectation Store" /> to your <TechnicalTag tag="data_context" text="Data Context" /> by using the `add_store()` metho by using the `add_store()` methodd.

```python name="tests/integration/docusaurus/deployment_patterns/aws_cloud_storage_pandas.py add_expectation_store"
```

This configuration tells Great Expectations to look for Expectations in a store called ``expectations_store``. The ``base_directory`` for ``expectations_store`` is set to ``expectations/`` by default.