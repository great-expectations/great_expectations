<!--
---Import---
import EnsureLatestPipVersion from './_ensure_latest_pip.mdx'
<EnsureLatestPipVersion />

---Header---
### Ensure you have the latest version of pip installed
-->

After you've activated your virtual environment, you should ensure that you have the latest version of pip installed. Pip is a tool that is used to easily install Python packages.  

Run the following command to ensure that you have the latest version of pip installed:

```bash title="Terminal command"
python -m ensurepip --upgrade
```

or

```bash title="Terminal command"
python3 -m ensurepip --upgrade
```