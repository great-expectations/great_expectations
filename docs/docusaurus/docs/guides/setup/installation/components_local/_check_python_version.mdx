import PythonVersion from '/docs/components/_python_version.mdx';

First, check the version of Python that you have installed.  Great Expectations supports <PythonVersion />.

You can check your version of Python by running:

```console title="Terminal command"
python --version
```

If this command returns something other than a Python 3 version number (like Python 3.X.X), you may need to try this:

```console title="Terminal command"
python3 --version
```
