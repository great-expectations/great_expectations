import PythonVersion from '/docs/components/_python_version.mdx';

Run the following code to check what version of Python is currently installed:

```console title="Terminal command"
python --version
```

Great Expectations supports <PythonVersion />. If a Python 3 version number is not returned, run the following code:

```console title="Terminal command"
python3 --version
```
