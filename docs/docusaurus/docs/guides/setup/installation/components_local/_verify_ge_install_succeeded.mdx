<!--
-->
import VersionSnippet from '/docs/components/versions/_gx_version_code_box.mdx'

Run the following command to confirm the GX installation is working:

```console title="Terminal command"
great_expectations --version
```

Version information similar to the following is returned:

<VersionSnippet />