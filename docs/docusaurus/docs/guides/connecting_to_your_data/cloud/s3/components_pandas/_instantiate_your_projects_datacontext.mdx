
Run the following command to import the packages and modules:

```python name="tests/integration/docusaurus/connecting_to_your_data/cloud/s3/pandas/inferred_and_runtime_yaml_example.py imports"
```

Run the following command to load your DataContext into memory with the `get_context()` method:

```python name="tests/integration/docusaurus/connecting_to_your_data/cloud/s3/pandas/inferred_and_runtime_yaml_example.py get_context"
```
