<!-- props.data_connector_type: string value of the data connector's class name. -->
<!-- props.data_connector_name: string value of the name assigned to the data connector in previous examples. -->
<!-- props.runtime: boolean value that determines if the base key list should be `class_name`, `base_name`, `default_regex` (false) OR `class_name`, `batch_identifiers` (true)
<!-- props.batch_spec_passtrhough: boolean value that determines if the batch_spec_passthrough key is included in the overview. -->
<!-- props.glob_directive: boolean value that determines if the guidance for glob_directive is included in the overview. -->

<div>
    <p>
        When defining an <code>{props.data_connector_type}</code> you will need to provide values for {props.runtime && <span>two</span> || props.batch_spec_passthrough && <span>four</span> || <span>three</span> } keys in the Data Connector's configuration dictionary (the currently empty dictionary that corresponds to <code>"{props.data_connector_name}"</code> in the example above).  These key/value pairs consist of:
    </p>
    <ul>
        <li>
            <code>class_name</code>: The name of the Class that will be instantiated for this <code>DataConnector</code>.
        </li>
        {props.runtime &&
            <li>
                <code>batch_identifiers</code>: A list of strings that will be used as keys for identifying metadata that the user provides for the returned Batch.
            </li>
            ||
            <span>
                <li>
                    <code>base_directory</code>: The string representation of the directory that contains your filesystem
                    data.
                </li>
                <li>
                <code>default_regex</code>: A dictionary that describes how the data should be grouped into Batches.</li>
                {props.batch_spec_passthrough &&
                    <li>
                    <code>batch_spec_passthrough</code>: A dictionary of values that are passed to the Execution Engine's backend.
                    </li>
                }
            </span>
        }
    </ul>
</div>

<div>
    {props.glob_directive &&
    <p>Additionally, you may optionally choose to define:
        <ul>
            <li>
                <code>glob_directive</code>: A regular expression that can be used to access source data files contained in subfolders of your <code>base_directory</code>.  If this is not defined, the default value of <code>*</code> will cause you Data Connector to only look at files in the <code>base_directory</code> itself.
            </li>
        </ul>
    </p> }
</div>