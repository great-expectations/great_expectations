import TipWhichDataConnectorToUse from '../components/_tip_which_data_connector_to_use.mdx'

<!-- props.backend: The backend used by the datasource's Execution Engine. -->
<p>
    For each Data Connector configuration, you will need to specify which type of Data Connector you will be using.  When using {props.backend} to work with data in a file system, the most likely ones will be the <code>InferredAssetFilesystemDataConnector</code>, the <code>ConfiguredAssetFilesystemDataConnector</code>, and the <code>RuntimeDataConnector</code>.
</p>

<p>
     If you are working with {props.backend} but not working with a file system, please see our <a href={"../#cloud"}>cloud specific guides</a> for more information.
</p>

<TipWhichDataConnectorToUse />